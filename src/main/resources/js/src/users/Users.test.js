import React from 'react';
import { shallow } from 'enzyme';
import Users from './Users';

it('renders without crashing', () => {
//  ReactDOM.render(<Users />, div);
});

it('renders a label and a drop down box', () => {
	
});
